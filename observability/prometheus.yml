global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: entain-balance
    static_configs:
      - targets: ["app:8080"]
    metrics_path: /metrics
